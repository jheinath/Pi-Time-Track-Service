<div class="sidebarNav">
    <div class="top-row pl-4 navbar">
        <a class="navbar-brand" href="">&#127856; Time Track</a>
        <button class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>

    <div @onclick="ToggleNavMenu">
        <ul class="nav flex-column">
            <li class="nav-item px-3">
                <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                    <span class="oi oi-home" aria-hidden="true"></span> Timesheet
                </NavLink>
            </li>
            <li class="nav-item px-3">
                <NavLink class="nav-link" href="/configuration" Match="NavLinkMatch.All">
                    <span class="oi oi-compass" aria-hidden="true"></span> Configuration
                </NavLink>
            </li>
        </ul>
    </div>
</div>


@code {
    private bool collapseNavMenu = true;

    private string NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
